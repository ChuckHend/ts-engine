# 4cast

TODO:
1) Early stopping on lstm.fit (once returns diminish, exit the fit process)
2) Use static and timeseries variables to separate layers in LSTM (see Dima)
3) Grid search; lstm parameters, n_in/n_out tuning, 
